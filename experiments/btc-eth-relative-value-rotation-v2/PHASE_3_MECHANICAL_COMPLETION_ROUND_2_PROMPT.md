# Relative-value v2 Phase 3 final mechanical completion round

You are the implementation agent for the second and final bounded pre-result mechanical completion round for `btc-eth-relative-value-rotation-v2`. Work only in `/home/vertico/crypto-strategy-control`. Read `AGENTS.md`, the Phase 3 round-2 authorization, frozen `PREREGISTRATION.json`, effective `PREREGISTRATION_DRAFT.json`, the Phase 2 terminal validation and audit, Phase 3 production preflight, round-1 model result/failure/parent validation, all relative-value source modules, and all relative-value tests before editing.

Do not run Git commands. Do not open `/home/vertico/crypto-direction-lab`, market files, any 2026/holdout path or metadata, or calculate aggregate returns/performance. Do not use GPU, network, credentials, orders, or capital. The parent will run real data only after source validation.

This round must complete the shortest contract-faithful pre-economic production and evaluator path, not merely make generic tests pass. Preserve all frozen strategy, cost, clock, fold, statistical, gate, multiplicity, holdout, and prospective rules. The prior +41.53% is contaminated evidence only and must not influence implementation choices.

First repair the measured round-1 failures. `build_production_bindings` must consume the immutable boundary-specific `ProductionRowIndex` (or an equivalent exact source-row interface), require its boundary to equal `end`, and bind each complete session's two observations to the exact retained close rows at the frozen session close. Use the actual per-asset `MinuteRow.identity`; never construct `session_hash:asset`. Preserve the true event timestamp and the full causal availability cutoff. Validate positive finite closes, complete named mappings, causal timestamps, unique ordered sessions/fills, exact base/delayed identities, and suffix isolation. Select the actual terminal fill exactly once before the output loop. The parent instrumentation must observe zero of 1,094 observation identity mismatches and work linear in 549 sessions plus 356 fills, not 127,092 fill-element iterations.

Then finish every still-open mechanical obligation identified by the Phase 2 terminal audit and round-1 source review: exact due-session identity; independent base/delayed clocks; post-due-fill decisions; immutable pending targets; cash/exposed quarantine precedence; priced liquidation or fail-closed unpriceable exposure; no gap bridge; exactly 150 newly complete sessions; segment and regime reset; actual last canonical terminal replacement; self-financing interval accrual, turnover, 14/28-bps costs, attribution and terminal wealth; strict aggregate and four-fold boundary-specific indices; separate warmup versus PnL state; all seven trials and stresses with independent state.

Complete the pure/no-I/O formal evaluator assembly needed immediately after fidelity approval. It must derive rather than hardcode baseline, fold, asset, parameter-neighbor, doubled-cost, delay, regime, completed-entry, concentration/exceptional-interval, bootstrap uncertainty, exact daily-Sharpe multiplicity registry, DSR, PBO, nineteen gates, leakage and governance evidence. Do not emit or calculate a real market result in this invocation. A production driver may accept verified in-memory inputs but must not contain an unconditional placeholder raise.

Expand the independent oracle without importing or calling production calculation helpers. Add behavioral production-versus-oracle fixtures for signals, targets, base/delayed transitions, gaps while cash and exposed, priced liquidation, missing-price failure, 149/150 recovery, every fold boundary, final ordinary fill, terminal replacement, costs, interval returns, attribution, wealth, bootstrap/statistics and gates. Replace or supplement superficial tests with exact behavior; test names alone are not evidence.

Run focused tests, the complete repository suite, Ruff, strict Mypy over `src`, and compilation. Report every file changed, exact commands/results, and any unresolved contract obligation. If complete fidelity cannot be established, say so explicitly; do not claim success from controller tests.
