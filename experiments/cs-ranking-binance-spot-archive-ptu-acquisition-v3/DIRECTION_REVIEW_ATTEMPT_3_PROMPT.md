You are the independent Phase 2 research-direction auditor for `cs-ranking-binance-spot-archive-ptu-acquisition-v3`.

Verify first that the repository is clean, record the exact 40-hex HEAD in the output, and verify the authorization's source-parent relationship and bound files. Follow `AGENTS.md` and `DIRECTION_REVIEW_ATTEMPT_3_AUTHORIZATION.json` exactly. Read every path in its `open_exactly` list. For each `hash_without_opening` path, compute SHA-256 only; do not print, parse, search, or semantically inspect its contents. Never open the rejected v1 `SYMBOL_MANIFEST.json` or inspect an embedded 2026 identity.

Recompute all bound hashes. Audit exact draft canonical SHA-256 `144d795799304945e115b9006d54fc6490b31dc00c3c0e485094980bbab21c0e`. Return one complete verdict covering at least these sixteen checks:

1. immutable prior verdicts and permanent v1-manifest ineligibility;
2. every v3 observation requires new retained official bytes;
3. raw XML binds decoded CommonPrefixes, pagination, and ObjectRecords;
4. exact root and 101 month queries cannot admit a 2026 market object or full-symbol object listing;
5. keys, ETags, sizes, last-modified, ZIP, checksum bytes, calculated hashes, and timestamps cross-bind;
6. ZIP safety and exact CSV/header/timestamp/Decimal/OHLCV validation are executable;
7. duplicate rows/keys, revisions, missing months/days, malformed data, and count arithmetic fail closed;
8. raw-only independent reconstruction and resume identities close the v2 production-path failure;
9. symbol reuse, rename/migration, gaps, episode splitting, and fresh 30-bar recovery are conservative;
10. membership and liquidity use only completed rows strictly before the signal;
11. retrospective last-bar knowledge cannot remove earlier membership;
12. delisting, base next-bar, delay, terminal, and multi-asset synchronization never forward-scan;
13. formal archive completeness and asset identity are not overstated;
14. sufficiency gates are ex ante and meaningful for a later cross-sectional baseline;
15. requests, bytes, storage, cycles, memory, retries, timeouts, repair, audit, and failure precedence are bounded and non-economic;
16. `DATA_CONTRACT_GO` permits only a separately preregistered baseline and cannot imply returns, model quality, holdout access, trading, credentials, or capital.

Return `DIRECTION_ACCEPTED` only if no specification or methodology revision is required before freeze. Otherwise return `REVISION_REQUIRED` and enumerate every known material finite correction now, with exact clause, problem, required change, and rationale. Do not defer known issues.

Strictly read-only and repository-only: no file writes, tests that create caches, browser/network call, `data/raw`, external market data, holdout source, returns, training, backtest, profitability inference, implementation/acquisition authorization, credentials, order, capital, GPU, or mining action. The Codex service transport is not counted as audit network access.

Return only one JSON object conforming exactly to `DIRECTION_REVIEW_OUTPUT_SCHEMA.json`, including at least sixteen explicit check records.
