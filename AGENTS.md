# Research controller roles

This controller records model routing but does not silently downgrade models. Preferred roles are Sol/high for research direction and audit, Terra/medium for implementation, and Luna/low for deterministic data work. Only confirmed quota, rate-limit, or temporary availability failures allow the documented fallback order. No agent may access exchange credentials, create orders, authorize capital, or change a frozen rejection.
