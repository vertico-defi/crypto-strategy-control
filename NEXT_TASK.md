# Next Task

Commit the exact hash-bound production authorization checkpoint, then invoke `volatility-parity-development` once with the verified data-lab interpreter. It may open only the 36 allowlisted 2024–2025 partitions, must finish within 1,800 seconds, and has no repair or rerun allowance. Verify the resulting canonical hash, closed 2026 holdout, test/lint/type state, and every frozen gate; then commit the development evidence and spend call 3/4 on a read-only Sol/xhigh independent audit. Do not open the holdout even if development passes until that audit explicitly returns the frozen approval verdict.
