# Next Task

Commit the preserved dependency failure and single bounded repair, then rerun the unchanged `btc-eth-long-only-mean-reversion-v1` deterministic development evaluation with the existing verified data-lab interpreter and `PYTHONPATH=src`. Recompute preregistration and reused data-contract hashes, mechanically reject every 2026 partition, and keep the final holdout closed pending every gate and an independent Sol/xhigh audit.
