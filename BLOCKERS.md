# Blockers

- No current research blocker. Codex CLI 0.146.0 is authenticated and a read-only Sol/xhigh live smoke test passed on 2026-08-02; production invocations are now explicit and audited.
- The exact `cs-ranking-ptu-data-audit-v1` experiment remains `DATA_NO_GO`; the distinct official Binance spot archive-observed universe route is active and unresolved.
- Source publication remains pending because this controller has no Git remote. The user has authorized public repository creation only after a successful secret and publication-safety scan.
- User-scope systemd inspection is unavailable inside the restricted Goal sandbox. Continuation unit files are versioned but deliberately not installed or enabled during active Goal ownership.
